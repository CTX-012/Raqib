Diff in /home/faisal/edge_monitor/src/exit_classify.rs:101:
     let pid_token_b = format!("pid={}", pid); // newer kernels use this form
     for line in &ctx.dmesg_lines {
         let lower = line.to_ascii_lowercase();
[31m-        let mentions_oom =
(B[m[31m-            lower.contains("out of memory") || lower.contains("oom-kill") || lower.contains("oom_reaper");
(B[m[32m+        let mentions_oom = lower.contains("out of memory")
(B[m[32m+            || lower.contains("oom-kill")
(B[m[32m+            || lower.contains("oom_reaper");
(B[m         if mentions_oom && (line.contains(&pid_token_a) || line.contains(&pid_token_b)) {
             return true;
         }
Diff in /home/faisal/edge_monitor/src/exit_classify.rs:230:
     #[test]
     fn kernel_oom_detected_via_dmesg_pid() {
         let mut ctx = ExitContext::default();
[31m-        ctx.dmesg_lines.push(
(B[m[31m-            "[12345.678] Out of memory: Killed process 1234 (python) total-vm:9000".into(),
(B[m[31m-        );
(B[m[32m+        ctx.dmesg_lines
(B[m[32m+            .push("[12345.678] Out of memory: Killed process 1234 (python) total-vm:9000".into());
(B[m         let r = classify_exit(&summary(Some(9), None), &ctx);
         assert_eq!(
             r,
Diff in /home/faisal/edge_monitor/src/runtime.rs:558:
             // signals get tracked: in dry-run the process exits on
             // its own and we still want correct attribution if it
             // happens to die during the same window.
[31m-            self.governor_killed_pids
(B[m[31m-                .insert(*pid, reason.clone());
(B[m[32m+            self.governor_killed_pids.insert(*pid, reason.clone());
(B[m 
             let name = self
                 .state
